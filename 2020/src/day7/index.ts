/* eslint-disable no-console */
function countContainsBagColor(rules: string, bagColor: string): number {
  console.log({ rules, bagColor });

  return 1;
}

function main() {

}

export { main, countContainsBagColor };
